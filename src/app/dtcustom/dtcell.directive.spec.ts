import { DtcellDirective } from './dtcell/dtcell.directive';

describe('DtcellDirective', () => {
  it('should create an instance', () => {
    const directive = new DtcellDirective();
    expect(directive).toBeTruthy();
  });
});
