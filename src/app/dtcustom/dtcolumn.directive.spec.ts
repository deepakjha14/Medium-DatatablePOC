import { DtcolumnDirective } from './dtcolumn/dtcolumn.directive';

describe('DtcolumnDirective', () => {
  it('should create an instance', () => {
    const directive = new DtcolumnDirective();
    expect(directive).toBeTruthy();
  });
});
