import { DtrowDirective } from './dtrow.directive';

describe('DtrowDirective', () => {
  it('should create an instance', () => {
    const directive = new DtrowDirective();
    expect(directive).toBeTruthy();
  });
});
